{
  "name": "get-stam-py",
  "version": "1.0.0",
  "scripts": {
    "build": "cd getstam-react && npm install && npm run build",
    "heroku-postbuild": "cd getstam-react && npm install && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
